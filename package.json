{
  "name": "sql-format",
  "version": "0.1.0",
  "description": "T-SQL formatter driven by RedGate-compatible style configs",
  "main": "dist/index.js",
  "bin": {
    "sql-format": "./bin/sql-format"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
